# Netlify runs Next.js via the OpenNext adapter (no @netlify/plugin-nextjs needed).
# https://docs.netlify.com/build/frameworks/framework-setup-guides/nextjs/overview/

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22"
